plugins {
  id "com.github.node-gradle.node" version "2.2.4"
}

node {
  version = '12.19.0'
  npmVersion = '6.14.9'
  download = true
}

task start(type: NpmTask) {
  args = ['run', 'start']
}

task build(type: NpmTask) {
  args = ['run', 'build']
}
